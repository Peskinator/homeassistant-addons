name: "Cleanuparr"
slug: "cleanuparr"
version: "0.1"
logo: "https://raw.githubusercontent.com/Cleanuparr/Cleanuparr/main/Logo/256.png"
description: >
  Cleanuparr: automatic cleanup tool for Sonarr/Radarr/Lidarr.
url: "https://github.com/Cleanuparr/Cleanuparr"

arch:
  - amd64

startup: application
boot: auto

# Expose the Cleanuparr web UI
ports:
  "11011/tcp": 11011
ports_description:
  "11011/tcp": "Cleanuparr web interface"

# Web UI / health checks
webui: "http://[HOST]:[PORT:11011]/"
watchdog: "tcp://[HOST]:[PORT:11011]"

# Environment values MUST be strings
environment:
  PORT: "11011"
  LOG_LEVEL: "info"
  DATABASE_PATH: "/data/cleanuparr.db"
  TZ: "America/New_York"

# IMPORTANT: no tag here — Supervisor will append :<version> when appropriate
image: "ghcr.io/cleanuparr/cleanuparr"

# Treat this as a generic container image
legacy: true
